# FIRST TIME ONLY

git init

# Create .gitignore file and add:
# *.exe
# *.out
# *.o

git add .

git commit -m "Initial Codeforces upload"

git branch -M main

git remote add origin YOUR_REPO_URL

git push -u origin main


# FUTURE USE ONLY THESE 3 COMMANDS

git add .

git commit -m "Added new problems"

git push



git switch data
git add data1.csv data2.csv
git commit -m "Updated datasets"
git push origin data

git switch AI
git add model1.py model2.py
git commit -m "Updated AI models"
git push origin AI


git branch
